services:

  postgres:
    image: postgres:17
    container_name: kycflow-postgres

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: kycflow

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: kycflow-redis

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

  backend:
    build:
      context: ./backend

    container_name: kycflow-backend

    ports:
      - "5000:5000"

    env_file:
      - ./backend/.env

    depends_on:
      - postgres
      - redis

  worker:
    build:
      context: ./backend

    container_name: kycflow-worker

    command: npm run worker

    env_file:
      - ./backend/.env

    depends_on:
      - redis
      - postgres

volumes:
  postgres_data:
  redis_data: